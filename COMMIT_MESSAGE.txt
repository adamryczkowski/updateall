feat(core,cli,plugins): implement Milestone 1 - MVP foundation

Implement the core MVP functionality for update-all:

## Core Module
- Add ConfigManager with YAML configuration support (XDG spec)
- Add YamlConfigLoader for loading/saving YAML config files
- Add Orchestrator for sequential plugin execution
- Add RunResult model for tracking execution results
- Add comprehensive tests (28 new tests)

## CLI Module
- Integrate orchestrator with run command
- Add check command for checking available updates
- Add status command for plugin status display
- Add history command with stats integration
- Add plugins subcommands (list, enable, disable)
- Add config subcommands (show, init, path)
- Add Rich progress display integration
- Update tests for new commands

## Plugins Module
- Add register_builtin_plugins() helper function
- Export register_builtin_plugins in __all__

## Pre-commit
- Exclude cli/ from mypy in pre-commit (uses local path deps)
- CLI mypy checking is done via poetry in subproject

All 106 tests pass across all subprojects.
